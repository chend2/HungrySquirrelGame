package hungrysquirrelgame;

/*The movable interface contains one abstract method, move. The move
method is defined in the Squirrel class, which implements Movable.*/

public interface Movable {
    void move (char direction);
}
